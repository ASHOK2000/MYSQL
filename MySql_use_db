MYSQL
Assuming  the company/DB  name is XYZ and the table is an employee 

mysql> mysql -u root -p     //then enter your password ex. MyNewPass

mysql> SHOW DATABASES;      // show list of dartabases

mysql> USE db_Name;        // switch to database 

mysql>  create table employee(
    -> id int primary key,
    -> name varchar(50),
    -> salary float);

mysql> SHOW TABLES;        // show all table inside database

mysql> insert into employee values(1,"Ashok",150000.00);

mysql> SELECT * FROM Table_Name   // show data inside the table 

mysql> SELECT  field1 , field2 from Table_Name  //show the selected data inside the table 

